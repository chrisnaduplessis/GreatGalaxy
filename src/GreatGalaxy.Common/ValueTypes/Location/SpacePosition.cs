namespace GreatGalaxy.Common.ValueTypes.Location
{
    // This class represents a position in space. It is not yet clear how this would work, but it could be something like a set of coordinates or a reference to a specific location in space.
    public record SpacePosition(string Position);
}
